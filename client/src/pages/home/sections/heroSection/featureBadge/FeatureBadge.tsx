import { Badge } from '@/components/ui/badge';

const FeatureBadge = () => (
    <Badge variant="outline" className="mb-6 text-yellow-300 border-yellow-300">
        New: Analytics are now available!
    </Badge>
);

export default FeatureBadge;
